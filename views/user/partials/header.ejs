<header class="header-area header-style-3 header-height-2">
    <!-- Top Bar -->
    <div class="header-top header-top-ptb-1 d-none d-lg-block">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-xl-3 col-lg-4">
                    <div class="header-info"></div>
                </div>
                <div class="col-xl-6 col-lg-4"></div>
                <div class="col-xl-3 col-lg-4">
                    <div class="header-info header-info-right">
                        <ul>
                            <li id="login-signup-desktop">
                                <i class="fi-rs-user"></i>
                                <a href="/login">Log In</a> /
                                <a href="/register">Sign Up</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- ✅ Combined Middle + Bottom -->
    <div class="header-bottom header-bottom-bg-color sticky-bar">
        <div class="container">
            <div class="header-wrap header-space-between position-relative main-nav">
                <!-- Logo (desktop + mobile) -->
                <div class="logo logo-width-1">
                    <a href="index.html"><span>sareesoul</span></a>
                </div>

                <!-- Nav + Wishlist/Cart together -->
                <div class="header-nav d-none d-lg-flex" style="width: 100%;">
                    <!-- Navigation -->
                    <div class="main-menu main-menu-padding-1 main-menu-lh-2 d-none d-lg-block">
                        <nav>
                            <ul>
                                <li><a class="active" href="index.html">Home</a></li>
                                <li><a href="/shop">Shop</a></li>
                                <li><a href="page-about.html">About Us</a></li>
                            </ul>
                        </nav>
                    </div>

                    <!-- Wishlist & Cart (pushed to right) -->
                    <div class="header-action-2" style="margin-left: auto;">
                        <div class="header-action-icon-2">
                            <a href="shop-wishlist.html">
                                <i class="fi-rs-heart"></i>
                                <span class="pro-count blue">4</span>
                            </a>
                        </div>
                        <div class="header-action-icon-2">
                            <a class="mini-cart-icon" href="shop-cart.html">
                                <i class="fi-rs-shopping-cart"></i>
                                <span class="pro-count blue">2</span>
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Mobile view -->
                <p class="mobile-promotion">Happy <span class="text-brand">Mother's Day</span>. Big Sale Up to 40%</p>
                <div class="header-action-right d-block d-lg-none">
                    <div class="header-action-2">
                        <div class="header-action-icon-2">
                            <a href="shop-wishlist.html">
                                <i class="fi-rs-heart"></i>
                                <span class="pro-count white">4</span>
                            </a>
                        </div>
                        <div class="header-action-icon-2">
                            <a class="mini-cart-icon" href="shop-cart.html">
                                <i class="fi-rs-shopping-cart"></i>
                                <span class="pro-count white">2</span>
                            </a>
                        </div>
                        <div class="header-action-icon-2 d-block d-lg-none">
                            <div class="burger-icon burger-icon-white">
                                <span class="burger-icon-top"></span>
                                <span class="burger-icon-mid"></span>
                                <span class="burger-icon-bottom"></span>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /Mobile -->
            </div>
        </div>
    </div>

    <!-- Script for Auth Links -->
    <script>
    (function() {
        const userData = <%- JSON.stringify(user) %>;
        console.log(userData)

        function renderAuthLinks() {
            const desktop = document.getElementById('login-signup-desktop');
            const mobile = document.getElementById('login-signup-mobile');
            const mobileInfo = document.getElementById('login-signup-mobile-info');

            if (userData) {
                //logged in
                if (desktop) {
                    desktop.innerHTML = `
                        <i class="fi-rs-user"></i>
                        <a href="/account">Welcome, ${userData.firstName}</a>
                        | <a href="/logout">Logout</a>
                    `;
                }
                if (mobile) {
                    mobile.innerHTML = `<a href="/account">Welcome, ${userData.firstName}</a> | <a href="/logout">Logout</a>`;
                }
                if (mobileInfo) {
                    mobileInfo.innerHTML = `<a href="/account">Welcome, ${userData.firstName}</a> | <a href="/logout">Logout</a>`;
                }
            } else {
                //not logged in
                if (desktop) {
                    desktop.innerHTML = `
                        <i class="fi-rs-user"></i>
                        <a href="/login">Log In</a> / <a href="/register">Sign Up</a>
                    `;
                }
                if (mobile) {
                    mobile.innerHTML = `<a href="/login">Log In</a> / <a href="/register">Sign Up</a>`;
                }
                if (mobileInfo) {
                    mobileInfo.innerHTML = `<a href="/login">Log In</a> / <a href="/register">Sign Up</a>`;
                }
            }
        }

        renderAuthLinks();
    })();
    </script>
</header>
