<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Verify OTP</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>

<body class="bg-light">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow p-4">
                    <h3 class="mb-4 text-center">Email Verification</h3>

                    <% if (error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                        <% } %>

                            <form action="/verify-otp" method="POST">
                                <input type="hidden" name="email" value="<%= email %>">

                                <div class="mb-3">
                                    <label for="otp" class="form-label">Enter OTP</label>
                                    <input type="text" class="form-control" id="otp" name="otp" maxlength="6" required>
                                </div>

                                <div class="d-grid">
                                    <button type="submit" class="btn btn-primary">Verify</button>
                                </div>
                                <div class="text-danger mt-2">
                                    <span id="timer">01:00</span> until OTP expires.
                                </div>

                            </form>

                            <div class="mt-3 text-center">
                                <small>Didn't receive OTP?
                                    <a href="/resend-otp?email=<%= email %>" id="resend-link"
                                        style="pointer-events: none; color: gray;">
                                        Resend
                                    </a>
                                </small>
                            </div>

                </div>
            </div>
        </div>
    </div>
    <script>
  let countdown = 1 * 60; // 2 minutes
  const timerDisplay = document.getElementById("timer");
  const resendLink = document.getElementById("resend-link");

  const updateTimer = () => {
    const minutes = String(Math.floor(countdown / 60)).padStart(2, '0');
    const seconds = String(countdown % 60).padStart(2, '0');
    timerDisplay.textContent = `${minutes}:${seconds}`;
    countdown--;

    if (countdown < 0) {
      clearInterval(timerInterval);
      timerDisplay.textContent = "Expired";

      // ✅ Enable Resend link
      resendLink.style.pointerEvents = "auto";
      resendLink.style.color = "#0d6efd"; // Bootstrap blue
      resendLink.style.textDecoration = "underline";
    }
  };

  updateTimer(); // initial call
  const timerInterval = setInterval(updateTimer, 1000);
</script>


</body>

</html>