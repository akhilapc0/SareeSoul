<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Forgot Password - sareesoul</title>
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="description" content="Reset your password">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta property="og:title" content="Forgot Password">
    <meta property="og:type" content="website">
    <link rel="shortcut icon" type="image/x-icon" href="/user/assets/imgs/theme/favicon.svg">
    <link rel="stylesheet" href="/user/assets/css/main.css">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .forgot-password-wrapper {
            min-height: calc(100vh - 400px);
            display: flex;
            align-items: center;
            padding: 60px 0;
        }

        .forgot-password-container {
            background: white;
            padding: 50px 40px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            max-width: 450px;
            margin: 0 auto;
        }

        .forgot-password-container h2 {
            color: #222;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 10px;
            text-align: center;
        }

        .forgot-password-subtitle {
            color: #999;
            font-size: 14px;
            text-align: center;
            margin-bottom: 35px;
            line-height: 1.6;
        }

        .forgot-password-form .form-group {
            margin-bottom: 25px;
        }

        .forgot-password-form label {
            display: block;
            color: #222;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 10px;
            text-transform: capitalize;
        }

        .forgot-password-form input {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #e0e0e0;
            border-radius: 4px;
            font-size: 14px;
            transition: all 0.3s ease;
            font-family: inherit;
        }

        .forgot-password-form input:focus {
            outline: none;
            border-color: #2bcec0;
            box-shadow: 0 0 0 3px rgba(43, 206, 192, 0.1);
            background-color: #fff;
        }

        .forgot-password-form .btn-submit {
            width: 100%;
            padding: 13px;
            background: linear-gradient(135deg, #2bcec0 0%, #1fa39c 100%);
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .forgot-password-form .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(43, 206, 192, 0.4);
        }

        .forgot-password-footer {
            text-align: center;
            margin-top: 25px;
            padding-top: 25px;
            border-top: 1px solid #f0f0f0;
        }

        .forgot-password-footer p {
            color: #666;
            font-size: 14px;
            margin: 0;
        }

        .forgot-password-footer a {
            color: #2bcec0;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .forgot-password-footer a:hover {
            color: #1fa39c;
            text-decoration: underline;
        }

        .alert {
            padding: 15px 18px;
            border-radius: 4px;
            margin-bottom: 20px;
            font-size: 14px;
            animation: slideDown 0.4s ease;
        }

        .alert-danger {
            background-color: #fff5f5;
            color: #c33;
            border-left: 4px solid #c33;
        }

        .alert-success {
            background-color: #f0fdf9;
            color: #2bcec0;
            border-left: 4px solid #2bcec0;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 768px) {
            .forgot-password-container {
                padding: 35px 25px;
                margin: 20px;
            }

            .forgot-password-container h2 {
                font-size: 24px;
            }

            .forgot-password-subtitle {
                font-size: 13px;
                margin-bottom: 30px;
            }
        }

        @media (max-width: 480px) {
            .forgot-password-wrapper {
                padding: 40px 0;
            }

            .forgot-password-container {
                padding: 30px 20px;
                margin: 15px;
            }

            .forgot-password-container h2 {
                font-size: 22px;
                margin-bottom: 8px;
            }

            .forgot-password-subtitle {
                margin-bottom: 25px;
            }
        }
    </style>
</head>
<body>
    <!-- Forgot Password Section -->
    <div class="forgot-password-wrapper">
        <div class="forgot-password-container">
            <h2>Forgot Password?</h2>
            <p class="forgot-password-subtitle">Enter your email address and we'll send you an OTP to reset your password.</p>

            <% if (error) { %>
                <div class="alert alert-danger">
                    <i class="fi-rs-info"></i> <%= error %>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="alert alert-success">
                    <i class="fi-rs-check"></i> <%= success %>
                </div>
            <% } %>

            <form class="forgot-password-form" action="/forgot-password" method="POST">
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input 
                        id="email" 
                        name="email" 
                        type="email" 
                        class="form-control" 
                        placeholder="your@email.com"
                        required
                        autocomplete="email"
                    >
                </div>
                <button type="submit" class="btn-submit">Send OTP</button>
            </form>

            <div class="forgot-password-footer">
                <p>Remember your password? <a href="/login">Back to Login</a></p>
                <p style="margin-top: 10px; color: #999;">Don't have an account? <a href="/register" style="color: #2bcec0;">Sign Up</a></p>
            </div>
        </div>
    </div>
</body>
</html>