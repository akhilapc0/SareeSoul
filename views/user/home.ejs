<!DOCTYPE html>
<html class="no-js" lang="en">
<%- include('./partials/head') %>

<style>
  /* Hero Section - Fixed visibility issue */
  .hero-section {
    position: relative;
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.4) 0%, rgba(118, 75, 162, 0.4) 100%),
                url('/user/assets/imgs/banner/homeishani.jpg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    color: white;
    padding: 120px 0;
    text-align: center;
    margin-bottom: 60px;
  }

  .hero-section h1 {
    font-size: 3.5rem;
    font-weight: 700;
    margin-bottom: 20px;
    text-shadow: 2px 2px 8px rgba(0,0,0,0.4);
    animation: fadeInUp 1s ease;
  }

  .hero-section p {
    font-size: 1.3rem;
    margin-bottom: 30px;
    text-shadow: 1px 1px 4px rgba(0,0,0,0.3);
    animation: fadeInUp 1.2s ease;
  }

  .hero-section .btn {
    animation: fadeInUp 1.4s ease;
    padding: 15px 40px;
    font-size: 1.1rem;
    border-radius: 50px;
    transition: all 0.3s ease;
  }

  .hero-section .btn:hover {
    transform: scale(1.05);
    box-shadow: 0 8px 20px rgba(0,0,0,0.2);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Offer Badge */
  .offer-badge {
    position: absolute;
    top: 15px;
    right: 15px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 8px 15px;
    border-radius: 25px;
    font-size: 0.9rem;
    font-weight: bold;
    z-index: 10;
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
  }

  /* Product Cards */
  .product-cart-wrap {
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    transition: all 0.3s ease;
    background: white;
  }

  .product-cart-wrap:hover {
    transform: translateY(-8px);
    box-shadow: 0 8px 25px rgba(102, 126, 234, 0.15);
  }

  .product-img-action-wrap {
    position: relative;
    overflow: hidden;
    height: 320px;
  }

  .product-img-action-wrap img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .product-cart-wrap:hover .product-img-action-wrap img {
    transform: scale(1.08);
  }

  .product-content-wrap {
    padding: 20px;
  }

  .product-category a {
    color: #667eea;
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .product-content-wrap h2 {
    font-size: 1.1rem;
    margin: 10px 0;
    height: 45px;
    overflow: hidden;
  }

  .product-content-wrap h2 a {
    color: #333;
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .product-content-wrap h2 a:hover {
    color: #667eea;
  }

  .product-price-display {
    margin: 15px 0;
  }

  .offer-price-text {
    color: #667eea;
    font-size: 1.4rem;
    font-weight: bold;
  }

  .original-price-text {
    text-decoration: line-through;
    color: #999;
    font-size: 1rem;
    margin-left: 10px;
  }

  .view-btn {
    display: inline-block;
    padding: 10px 25px;
    background: #667eea;
    color: white;
    border-radius: 25px;
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .view-btn:hover {
    background: #764ba2;
    color: white;
    transform: translateX(5px);
  }

  /* Features Section */
  .banner-features {
    text-align: center;
    padding: 30px 20px;
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    transition: all 0.3s ease;
  }

  .banner-features:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 20px rgba(102, 126, 234, 0.12);
  }

  .banner-features img {
    width: 60px;
    margin-bottom: 15px;
  }

  .banner-features h4 {
    font-size: 1rem;
    font-weight: 600;
    color: #333;
    margin: 0;
  }

  /* Section Headers */
  .section-header {
    text-align: center;
    margin-bottom: 50px;
  }

  .section-header h2 {
    font-size: 2.5rem;
    font-weight: 700;
    color: #333;
    margin-bottom: 15px;
  }

  .section-header p {
    font-size: 1.1rem;
    color: #666;
  }

  .view-all-link {
    color: #667eea;
    font-weight: 600;
    text-decoration: none;
    font-size: 1.1rem;
    transition: all 0.3s ease;
  }

  .view-all-link:hover {
    color: #764ba2;
    text-decoration: underline;
  }

  /* Call to Action */
  .cta-section {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 80px 0;
    text-align: center;
    margin-top: 60px;
  }

  .cta-section h2 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 20px;
  }

  .cta-section p {
    font-size: 1.2rem;
    margin-bottom: 30px;
  }

  .cta-section .btn {
    padding: 15px 50px;
    font-size: 1.1rem;
    border-radius: 50px;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-section h1 {
      font-size: 2rem;
    }
    .hero-section p {
      font-size: 1rem;
    }
    .product-img-action-wrap {
      height: 250px;
    }
  }
</style>

<body>
    <%- include('./partials/header') %>
    
    <main class="main">
        <!-- Hero Section with Background Image -->
        <section class="hero-section">
            <div class="container">
                <h1 class="wow fadeInUp">Welcome to Saree Soul</h1>
                <p class="wow fadeInUp" data-wow-delay="0.2s">Discover the finest collection of traditional and modern sarees</p>
                <a href="/shop" class="btn btn-light btn-lg wow fadeInUp" data-wow-delay="0.4s">
                  Shop Now <i class="fi-rs-arrow-right"></i>
                </a>
            </div>
        </section>

        <!-- Features Section -->
        <section class="featured section-padding">
            <div class="container">
                <div class="row g-4">
                    <div class="col-lg-3 col-md-6">
                        <div class="banner-features wow fadeIn animated hover-up">
                            <img src="/user/assets/imgs/theme/icons/feature-1.png" alt="Free Shipping">
                            <h4>Free Shipping</h4>
                        </div>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <div class="banner-features wow fadeIn animated hover-up" data-wow-delay="0.1s">
                            <img src="/user/assets/imgs/theme/icons/feature-2.png" alt="Online Order">
                            <h4>Online Order</h4>
                        </div>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <div class="banner-features wow fadeIn animated hover-up" data-wow-delay="0.2s">
                            <img src="/user/assets/imgs/theme/icons/feature-3.png" alt="Save Money">
                            <h4>Save Money</h4>
                        </div>
                    </div>
                    <div class="col-lg-3 col-md-6">
                        <div class="banner-features wow fadeIn animated hover-up" data-wow-delay="0.3s">
                            <img src="/user/assets/imgs/theme/icons/feature-4.png" alt="24/7 Support">
                            <h4>24/7 Support</h4>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- New Arrivals Section -->
        <section class="product-tabs section-padding">
            <div class="container">
                <div class="section-header wow fadeIn animated">
                    <h2>New Arrivals</h2>
                    <p>Explore our latest collection of beautiful sarees</p>
                </div>

                <div class="row product-grid-4">
                    <% if (featuredProducts && featuredProducts.length > 0) { %>
                      <% featuredProducts.forEach((product, index) => { %>
                        <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                            <div class="product-cart-wrap wow fadeInUp animated" data-wow-delay="<%= index * 0.1 %>s">
                                <div class="product-img-action-wrap">
                                    <a href="/product/<%= product._id %>">
                                        <img src="<%= product.image %>" alt="<%= product.name %>">
                                    </a>
                                    
                                    <!-- Offer Badge -->
                                    <% if (product.hasOffer) { %>
                                      <span class="offer-badge"><%= product.discount %>% OFF</span>
                                    <% } %>
                                </div>

                                <div class="product-content-wrap">
                                    <!-- Category -->
                                    <div class="product-category">
                                        <a href="/shop?category=<%= product.categoryId?.name %>">
                                          <%= product.categoryId?.name || 'Sarees' %>
                                        </a>
                                    </div>

                                    <!-- Product Name -->
                                    <h2>
                                        <a href="/product/<%= product._id %>"><%= product.name %></a>
                                    </h2>

                                    <!-- Price Display -->
                                    <div class="product-price-display">
                                        <% if (product.hasOffer) { %>
                                            <span class="offer-price-text">₹<%= product.offerPrice %></span>
                                            <span class="original-price-text">₹<%= product.salesPrice %></span>
                                        <% } else { %>
                                            <span class="offer-price-text">₹<%= product.salesPrice %></span>
                                        <% } %>
                                    </div>

                                    <!-- View Button -->
                                    <a href="/product/<%= product._id %>" class="view-btn">
                                        View Details <i class="fi-rs-arrow-right"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                      <% }) %>
                    <% } else { %>
                      <div class="col-12 text-center">
                        <p class="text-muted">No products available at the moment.</p>
                        <a href="/shop" class="btn btn-primary mt-3">Browse Shop</a>
                      </div>
                    <% } %>
                </div>

                <% if (featuredProducts && featuredProducts.length > 0) { %>
                  <div class="text-center mt-5 wow fadeIn animated">
                    <a href="/shop" class="view-all-link">
                      View All Sarees <i class="fi-rs-arrow-right"></i>
                    </a>
                  </div>
                <% } %>
            </div>
        </section>

        <!-- Call to Action Section -->
        <!-- <section class="cta-section wow fadeIn animated">
            <div class="container">
                <h2>Explore Our Complete Collection</h2>
                <p>Discover traditional and modern sarees for every occasion</p>
                <a href="/shop" class="btn btn-light btn-lg">
                  Shop All Sarees <i class="fi-rs-arrow-right"></i>
                </a>
            </div>
        </section> -->
    </main>

    <%- include('./partials/footer') %>
</body>
</html>